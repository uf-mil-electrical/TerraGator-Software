#pragma once

/*
@brief finds the median value of an array
@param arr pointer to array storing data to find median of 
@param n length of array 
@return median of the array's data 
*/
float median(float* arr, int n);